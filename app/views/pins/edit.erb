<h1>Edit Pin</h1>
<% if !@errors.nil? %>
  <div class="error"><i class="fa fa-exclamation-circle"></i>&nbsp; 
    <label><%= pluralize(@errors.count, "error") %> prohibited this pin from being saved:</label>
    <ul>
      <% @errors.full_messages.each do |msg| %>
        <li><%= "#{msg}" %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= render "form" %>